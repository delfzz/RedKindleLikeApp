import React from 'react'

const ShowFileDatas = ({fileData}) => {

    return <div style={{justifyContent:'center',display:'flex',padding:80,textAlign:'justify',}}>
            <p >{fileData}</p>
    </div>
}

export default ShowFileDatas